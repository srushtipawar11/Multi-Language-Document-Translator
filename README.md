# Multi-Language-Document-Translator
Multi-Language Document Translator is an AI-powered application that translates documents from one language into multiple target languages while preserving the original content and formatting. It uses OCR and Natural Language Processing (NLP) to extract, translate, and generate translated documents efficiently.
